let variabel = 5;
let nama = 'Robert Baz Kiyosaki';
let bilangan = 0.5;
bilangan = 0.8;  // Nilai bilangan berubah menjadi 0.8

let tebakData = 5;
tebakData = 10;  // Nilai tebakData berubah menjadi 10
tebakData = 3;   // Nilai tebakData berubah menjadi 3
tebakData = tebakData + 12;  // Nilai tebakData sekarang 15 (3 + 12)
console.log(tebakData);  // Output: 15
